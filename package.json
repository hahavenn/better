{
  "name": "ha_edu",
  "module": "index.ts",
  "type": "module",
  "workspaces": [
    "user_main"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "lint": "clear && bun --filter '*' lint",
    "preview": "clear && bun --filter '*' preview"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.6.3"
  },
  "trustedDependencies": [
    "user_main"
  ]
}
