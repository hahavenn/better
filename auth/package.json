{
  "name": "auth",
  "type": "module",
  "scripts": {
    "dev": "rm .env && npm run devEnvs && npm run jwtSecret && node --watch --env-file=.env src/server.ts",
    "httpsKeyCert": "node scripts/httpsKeyCert.ts",
    "jwtSecret": "node scripts/jwtSecret.ts",
    "devEnvs": "node scripts/devEnvs.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "fastify": "^5.6.0",
    "mysql2": "^3.15.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.1"
  }
}
